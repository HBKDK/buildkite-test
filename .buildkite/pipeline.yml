steps:
  - label: "Hello Buildkite on K8s"
    command:
      - echo "=== Buildkite agent-stack-k8s Test ==="
      - hostname
      - echo "Running inside K8s Pod!"
    agents:
      queue: kubernetes
